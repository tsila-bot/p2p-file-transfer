{
  "name": "p2p-file-transfer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run --workspace backend dev\" \"npm run --workspace frontend dev\"",
    "build": "npm run --workspace shared build && npm run --workspace backend build && npm run --workspace frontend build",
    "test": "npm run --workspace backend test && npm run --workspace frontend test",
    "clean": "npm run --workspace frontend clean && npm run --workspace backend clean && npm run --workspace shared clean",
    "setup": "./scripts/setup.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0"
  }
}
